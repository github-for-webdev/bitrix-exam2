Документация по коду:

AddEventHandler(
    "Идентификатор модуля который будет инициировать событие",
    "Идентификатор события (из задачи это OnBeforeEventAdd)", - отлов события (cобытие OnBeforeEventAdd вызывается в момент добавления почтового события в таблицу b_event)
    Название функции обработчика. Если это метод класса, то массив вида Array(класс(объект), название метода).
)
CEventLog::Add() - записываем лог в систему

Выполнение билета:

Создать раздел "Экзамен2".
В этом разделе создать ещё раздел "Форма обратной связи", добавить в левое меню, на страницу добавить компонент "Форма обратной связи".

В папку local добавить папку php_interface, в ней же:
    1. файл init.php (где нужно подключить файл из папки include)
    2. папка include
        2.1 файл events.php (где будет реализована вся логика из задания)
        2.2. папка lang/ru

В events.php

\Bitrix\Main\EventManager::getInstance()->addEventHandler("main", "OnBeforeEventAdd", function (&$event, &$lid, &$arFields) {
    //event - почтовый шаблон (вывод: FEEDBACK_FORM)
    //lid - ID шаблона сайта (вывод: s1)
    //arFields - массив данных из инпутов в веб-форме

    1. Проверка: Произошло ли событие "Отправка формы" (FEEDBACK_FORM). Если нет, то выйти из функции.
    2. Проверка: Какой пользователь отправил письмо (авторизованный или нет). В зависимости от условия, $arFields["AUTHOR"] присвоить значение исходя из задачи.
    3. Добавить запись в журнал событий (CEventLog::Add)

    CEventLog::Add(
        array(
            "SEVERITY" => степень важности записи. Доступны значения: SECURITY, ERROR, INFO, DEBUG или WARNING, для иного система установит UNKNOWN
            "AUDIT_TYPE_ID"  => собственный ID типа события
            "MODULE_ID" => модуль, с которого происходит запись в лог
            "ITEM_ID" => ID объекта, в связи с которым происходит добавление (пользователь, элемент ИБ, ID сообщения, ...)
            "DESCRIPTION" => собственно описание записи лога, или техническая информация
        )
    );
});

Проверить лог: Рабочий стол/Настройки/Инструменты/Журнал событий
Проверить решение: Рабочий стол/Настройки/Производительность/Таблицы->b_event